<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order History - WaterWise</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        :root {
            --primary-blue: #0d6efd;
            --primary-blue-dark: #0a58ca;
            --light-blue-bg: #f0f7ff;
            --white-bg: #ffffff;
            --text-dark: #212529;
            --text-muted: #6c757d;
            --border-color: #e9ecef;
            --status-delivered-bg: #e6f9f0;
            --status-delivered-text: #1b8754;
            --status-enroute-bg: #fff3e0;
            --status-enroute-text: #ff9800;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-blue-bg);
            color: var(--text-dark);
        }

        .page-header {
            text-align: center;
            padding: 2rem 0;
            color: var(--primary-blue-dark);
            font-weight: 700;
        }

        .order-card {
            background-color: var(--white-bg);
            border-radius: 15px;
            border: 1px solid var(--border-color);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
            margin-bottom: 1.5rem;
            overflow: hidden;
        }

        .order-card-header {
            padding: 1rem 1.5rem;
            background-color: var(--light-blue-bg);
            border-bottom: 1px solid var(--border-color);
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 1rem;
            flex-wrap: wrap;
        }
        
        .order-card-header h5 {
            margin: 0;
            font-weight: 600;
            color: var(--primary-blue-dark);
        }
        
        .order-card-header strong {
            font-size: 0.9rem;
            color: var(--text-muted);
        }

        .order-card-body {
            padding: 1.5rem;
        }

        .order-detail {
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            margin-bottom: 1rem;
        }

        .order-detail i {
            margin-top: 5px;
            color: var(--primary-blue);
        }
        
        .order-detail strong {
            display: block;
            color: var(--text-dark);
            font-weight: 500;
        }
        
        .order-detail span {
            color: var(--text-muted);
        }
        
        .order-status {
            display: inline-block;
            padding: 0.3rem 0.8rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 0.8rem;
        }

        .status-delivered {
            background-color: var(--status-delivered-bg);
            color: var(--status-delivered-text);
        }
        
        .status-en-route {
            background-color: var(--status-enroute-bg);
            color: var(--status-enroute-text);
        }

        .no-orders-card {
            background-color: var(--white-bg);
            border-radius: 15px;
            padding: 3rem;
            text-align: center;
            border: 1px solid var(--border-color);
        }
        
        .no-orders-card h2 {
            color: var(--primary-blue-dark);
            font-weight: 600;
        }

        /* Responsive styles */
.page-header {
    padding: clamp(1.5rem, 3vw, 2rem) 1rem;
    font-size: clamp(1.75rem, 4vw, 2.5rem);
}

.order-card {
    border-radius: clamp(12px, 2vw, 15px);
    margin-bottom: clamp(1rem, 2vw, 1.5rem);
}

.order-card-header {
    padding: clamp(0.875rem, 2vw, 1.5rem);
}

.order-card-header h5 {
    font-size: clamp(1rem, 3vw, 1.25rem);
}

.order-card-header strong {
    font-size: clamp(0.8rem, 2vw, 0.9rem);
}

.order-card-body {
    padding: clamp(1.25rem, 3vw, 1.5rem);
}

.order-detail {
    font-size: clamp(0.875rem, 2vw, 1rem);
    gap: clamp(0.5rem, 2vw, 0.75rem);
    margin-bottom: clamp(0.75rem, 2vw, 1rem);
}

.order-status {
    padding: clamp(0.25rem, 1vw, 0.3rem) clamp(0.6rem, 2vw, 0.8rem);
    font-size: clamp(0.75rem, 1.8vw, 0.8rem);
}

.no-orders-card {
    padding: clamp(2rem, 5vw, 3rem);
    border-radius: clamp(12px, 2vw, 15px);
}

.no-orders-card h2 {
    font-size: clamp(1.5rem, 4vw, 2rem);
}

/* Tablet breakpoint */
@media (max-width: 768px) {
    .page-header {
        padding: 1.5rem 0.75rem;
    }
    
    .order-card-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 0.5rem;
    }
    
    .order-card-body {
        padding: 1.25rem;
    }
    
    .order-detail {
        margin-bottom: 0.875rem;
    }
}

/* Mobile breakpoint */
@media (max-width: 480px) {
    .page-header {
        padding: 1.25rem 0.5rem;
    }
    
    .order-card {
        border-radius: 12px;
        margin-bottom: 1rem;
    }
    
    .order-card-header {
        padding: 0.75rem 1rem;
        gap: 0.4rem;
    }
    
    .order-card-body {
        padding: 1rem;
    }
    
    .order-detail {
        gap: 0.5rem;
        margin-bottom: 0.75rem;
    }
    
    .order-detail i {
        margin-top: 3px;
        font-size: 0.9rem;
    }
    
    .order-status {
        display: block;
        text-align: center;
        margin-top: 0.25rem;
    }
    
    .no-orders-card {
        padding: 2rem 1rem;
        border-radius: 12px;
    }
}

/* Extra small devices */
@media (max-width: 360px) {
    .order-card-header {
        padding: 0.65rem 0.875rem;
    }
    
    .order-card-body {
        padding: 0.875rem;
    }
    
    .order-detail {
        font-size: 0.8125rem;
    }
}
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <main class="container my-4">
        <h1 class="page-header">My Order History</h1>

        <% if (orders && orders.length > 0) { %>
            <div class="order-list">
                <% orders.forEach(order => { %>
                    <div class="order-card">
                        <div class="order-card-header">
                            <div>
                                <h5>Supplier: <strong><%= order.business_name %></strong></h5>
                                <small class="text-muted">Order #<%= order.id %></small>
                            </div>
                            <strong><%= new Date(order.order_time).toLocaleDateString('en-IN', { day: 'numeric', month: 'long', year: 'numeric' }) %></strong>
                        </div>
                        <div class="order-card-body">
                            <div class="row align-items-center">
                                <div class="col-md-8">
                                    <div class="order-detail">
                                        <i class="fas fa-map-marker-alt"></i>
                                        <div>
                                            <strong>Delivery Address</strong>
                                            <span><%= order.delivery_address %></span>
                                        </div>
                                    </div>
                                    <div class="order-detail">
                                        <i class="fas fa-rupee-sign"></i>
                                        <div>
                                            <strong>Total Price</strong>
                                            <span>₹<%= parseFloat(order.total_price).toLocaleString('en-IN') %></span>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-4 text-md-end mt-3 mt-md-0">
                                    <% if (order.order_status === 'Delivered') { %>
                                        <span class="order-status status-delivered">Delivered</span>
                                    <% } else if (order.order_status === 'En-Route') { %>
                                        <span class="order-status status-en-route">En-Route</span>
                                        <a href="/track-order/<%= order.id %>" class="btn btn-primary btn-sm mt-2">Track Order</a>
                                    <% } else { %>
                                        <span class="order-status"><%= order.order_status %></span>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="no-orders-card">
                <h2>No History Found</h2>
                <p class="text-muted">You haven't placed any orders yet. Ready to get started?</p>
                <a href="/book-tanker" class="btn btn-primary mt-2">Book Your First Tanker</a>
            </div>
        <% } %>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        lucide.createIcons();
    </script>
</body>
</html>
