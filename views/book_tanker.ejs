<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book a Tanker - WaterWise</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        :root {
    --primary-blue: #0d6efd;
    --primary-blue-dark: #0a58ca;
    --light-blue-bg: #f0f7ff;
    --white-bg: #ffffff;
    --text-dark: #212529;
    --text-muted: #6c757d;
    --border-color: #e9ecef;
    --warning-bg: #fff8e1;
}

body {
    font-family: 'Poppins', sans-serif;
    background-color: var(--light-blue-bg);
    color: var(--text-dark);
    margin: 0;
    padding: 0;
}

.page-header {
    text-align: center;
    padding: 2rem 1rem;
}

.page-header h1 {
    color: var(--primary-blue-dark);
    font-weight: 700;
    font-size: clamp(1.75rem, 5vw, 2.5rem);
    margin-bottom: 0.5rem;
}

.page-header p {
    color: var(--text-muted);
    font-size: clamp(1rem, 2.5vw, 1.1rem);
}

.tanker-card {
    background-color: var(--white-bg);
    border-radius: 15px;
    border: 1px solid var(--border-color);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
    height: 100%;
    margin-bottom: 1.5rem;
}

.tanker-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
}

.card-header-custom {
    background-color: var(--light-blue-bg);
    padding: 1rem 1.5rem;
    border-bottom: 1px solid var(--border-color);
    border-radius: 15px 15px 0 0;
}

.card-header-custom h5 {
    font-weight: 600;
    color: var(--primary-blue-dark);
    margin-bottom: 0.25rem;
    font-size: clamp(1.1rem, 3vw, 1.25rem);
}

.rating-stars {
    color: #ffc107;
    font-size: clamp(0.85rem, 2vw, 0.9rem);
}

.card-body-custom {
    padding: 1.5rem;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
}

.info-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.75rem;
    color: var(--text-muted);
    font-size: clamp(0.875rem, 2vw, 1rem);
    flex-wrap: wrap;
    gap: 0.5rem;
}

.info-row strong {
    color: var(--text-dark);
    font-weight: 500;
}

.total-price-box {
    background-color: var(--light-blue-bg);
    border-radius: 10px;
    padding: 1rem;
    text-align: center;
    margin-top: auto;
    margin-bottom: 1.5rem;
}

.total-price-box p {
    margin: 0;
    color: var(--text-muted);
    font-size: clamp(0.85rem, 2vw, 0.9rem);
}

.total-price-box h4 {
    font-weight: 700;
    color: var(--primary-blue-dark);
    margin: 0;
    font-size: clamp(1.25rem, 3vw, 1.5rem);
}

.btn-book-now {
    background: linear-gradient(135deg, var(--primary-blue), var(--primary-blue-dark));
    border: none;
    font-weight: 600;
    padding: 0.75rem;
    transition: transform 0.2s ease;
    font-size: clamp(0.9rem, 2.5vw, 1rem);
    width: 100%;
}

.btn-book-now:hover {
    transform: scale(1.03);
}

.no-tankers-card {
    background-color: var(--white-bg);
    border-radius: 15px;
    padding: 2rem 1rem;
    text-align: center;
    border: 1px solid var(--border-color);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin: 1rem;
}

.no-tankers-card i {
    font-size: clamp(2.5rem, 8vw, 3rem);
    color: var(--primary-blue);
    margin-bottom: 1rem;
    background-color: var(--light-blue-bg);
    padding: 1.5rem;
    border-radius: 50%;
}

.no-tankers-card h2 {
    color: var(--primary-blue-dark);
    font-size: clamp(1.5rem, 4vw, 2rem);
}

.no-tankers-card p {
    font-size: clamp(0.9rem, 2.5vw, 1rem);
}

/* Tablet breakpoint */
@media (max-width: 768px) {
    .page-header {
        padding: 1.5rem 1rem;
    }
    
    .tanker-card {
        margin-bottom: 1rem;
    }
    
    .card-header-custom {
        padding: 0.875rem 1.25rem;
    }
    
    .card-body-custom {
        padding: 1.25rem;
    }
    
    .info-row {
        margin-bottom: 0.65rem;
    }
    
    .total-price-box {
        padding: 0.875rem;
        margin-bottom: 1.25rem;
    }
    
    .btn-book-now {
        padding: 0.65rem;
    }
}

/* Mobile breakpoint */
@media (max-width: 480px) {
    .page-header {
        padding: 1rem 0.75rem;
    }
    
    .tanker-card {
        border-radius: 12px;
        margin-bottom: 1rem;
    }
    
    .card-header-custom {
        padding: 0.75rem 1rem;
        border-radius: 12px 12px 0 0;
    }
    
    .card-body-custom {
        padding: 1rem;
    }
    
    .info-row {
        font-size: 0.875rem;
        margin-bottom: 0.6rem;
        gap: 0.25rem;
    }
    
    .total-price-box {
        padding: 0.75rem;
        border-radius: 8px;
        margin-bottom: 1rem;
    }
    
    .btn-book-now {
        padding: 0.6rem;
        font-size: 0.9rem;
    }
    
    .no-tankers-card {
        padding: 1.5rem 0.75rem;
        border-radius: 12px;
    }
    
    .no-tankers-card i {
        padding: 1.25rem;
    }
    
    .tanker-card:hover {
        transform: translateY(-4px);
    }
}

/* Extra small devices */
@media (max-width: 360px) {
    .card-header-custom {
        padding: 0.65rem 0.875rem;
    }
    
    .card-body-custom {
        padding: 0.875rem;
    }
    
    .info-row {
        font-size: 0.8125rem;
    }
    
    .btn-book-now {
        padding: 0.55rem;
        font-size: 0.875rem;
    }
}

/* Large desktop breakpoint */
@media (min-width: 1200px) {
    .page-header {
        padding: 3rem 0;
    }
    
    .no-tankers-card {
        padding: 4rem 2rem;
    }
}

    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <main class="container my-4">
        <div class="page-header">
            <h1>Available Water Tankers</h1>
            <p>Compare suppliers and book the one that's right for you.</p>
        </div>

        <div class="row g-4">
            <% if (tankers && tankers.length > 0) { %>
                <% tankers.forEach(tanker => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="tanker-card">
                            <div class="card-header-custom">
                                <h5><%= tanker.business_name %></h5>
                                <div class="rating-stars">
                                    <i class="fas fa-star"></i>
                                    <strong><%= parseFloat(tanker.average_rating).toFixed(1) %></strong>
                                    <span class="text-muted small">(<%= tanker.rating_count %> reviews)</span>
                                </div>
                            </div>
                            <div class="card-body-custom">
                                <div>
                                    <div class="info-row">
                                        <span>Capacity</span>
                                        <strong><%= tanker.capacity_litres.toLocaleString('en-IN') %> Litres</strong>
                                    </div>
                                    <div class="info-row">
                                        <span>Price / 1000L</span>
                                        <strong>₹<%= tanker.price_per_1000_litres.toLocaleString('en-IN') %></strong>
                                    </div>
                                </div>
                                
                                <div class="total-price-box">
                                    <p>Total Estimated Price</p>
                                    <h4>₹<%= ((tanker.capacity_litres / 1000) * tanker.price_per_1000_litres).toLocaleString('en-IN') %></h4>
                                </div>

                                <form action="/confirm-order" method="POST">
                                    <input type="hidden" name="tankerId" value="<%= tanker.id %>">
                                    <button type="submit" class="btn btn-primary w-100 fw-bold btn-book-now">Book Now</button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12">
                    <div class="no-tankers-card">
                        <i class="fas fa-info-circle"></i>
                        <h2 class="mt-3">No Tankers Available</h2>
                        <p class="text-muted">Sorry, no tankers are currently available in your area. Please check back later.</p>
                    </div>
                </div>
            <% } %>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        lucide.createIcons();
    </script>
</body>
</html>
