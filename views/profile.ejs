    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manage Profile - WaterWise</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        :root {
            --primary-blue: #0d6efd;
            --primary-blue-dark: #0a58ca;
            --light-blue-bg: #f0f7ff;
            --white-bg: #ffffff;
            --text-dark: #212529;
            --text-muted: #6c757d;
            --border-color: #e9ecef;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-blue-bg);
        }

        .profile-card {
            background-color: var(--white-bg);
            border-radius: 15px;
            border: 1px solid var(--border-color);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.07);
            overflow: hidden;
            margin-top: 2rem;
        }

        .card-header-custom {
            text-align: center;
            padding: 2rem;
            background-color: var(--light-blue-bg);
            border-bottom: 1px solid var(--border-color);
        }
        
        .card-header-custom h1 {
            color: var(--primary-blue-dark);
            font-weight: 700;
        }

        .card-body-custom {
            padding: 2rem 2.5rem;
        }

        .form-label {
            font-weight: 600;
            color: var(--primary-blue-dark);
        }
        
        .form-control {
            border-radius: 8px;
            padding: 0.75rem 1rem;
        }
        
        .form-control:focus {
            border-color: var(--primary-blue);
            box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
        }
        
        .password-note {
            font-size: 0.85rem;
            color: var(--text-muted);
        }

        .btn-update {
            background: linear-gradient(135deg, var(--primary-blue), var(--primary-blue-dark));
            border: none;
            font-weight: 600;
            padding: 0.8rem;
            font-size: 1.1rem;
            border-radius: 8px;
        }

        /* Responsive styles */
.profile-card {
    margin-top: clamp(1rem, 3vw, 2rem);
    border-radius: clamp(12px, 2vw, 15px);
}

.card-header-custom {
    padding: clamp(1.5rem, 3vw, 2rem);
}

.card-header-custom h1 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
}

.card-body-custom {
    padding: clamp(1.25rem, 4vw, 2.5rem);
}

.form-label {
    font-size: clamp(0.9rem, 2vw, 1rem);
}

.form-control {
    padding: clamp(0.65rem, 2vw, 0.75rem) clamp(0.875rem, 2vw, 1rem);
    font-size: clamp(0.9rem, 2vw, 1rem);
}

.password-note {
    font-size: clamp(0.8rem, 1.8vw, 0.85rem);
}

.btn-update {
    padding: clamp(0.7rem, 2vw, 0.8rem);
    font-size: clamp(0.95rem, 2.5vw, 1.1rem);
    width: 100%;
}

/* Tablet breakpoint */
@media (max-width: 768px) {
    .profile-card {
        margin-top: 1.5rem;
    }
    
    .card-header-custom {
        padding: 1.5rem 1rem;
    }
    
    .card-body-custom {
        padding: 1.5rem 1.25rem;
    }
}

/* Mobile breakpoint */
@media (max-width: 480px) {
    .profile-card {
        margin-top: 1rem;
        border-radius: 12px;
    }
    
    .card-header-custom {
        padding: 1.25rem 0.75rem;
    }
    
    .card-body-custom {
        padding: 1.25rem 1rem;
    }
    
    .form-label {
        font-size: 0.9rem;
    }
    
    .form-control {
        padding: 0.65rem 0.875rem;
        font-size: 0.9rem;
    }
    
    .password-note {
        font-size: 0.8rem;
    }
    
    .btn-update {
        padding: 0.7rem;
        font-size: 0.95rem;
    }
}

/* Extra small devices */
@media (max-width: 360px) {
    .card-body-custom {
        padding: 1rem 0.75rem;
    }
    
    .form-control {
        padding: 0.6rem 0.75rem;
        font-size: 0.875rem;
    }
}
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <main class="container my-4">
        <div class="row justify-content-center">
            <div class="col-lg-7 col-md-9">

                <% if (typeof message !== 'undefined' && message) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="fas fa-check-circle me-2"></i>
                        <%= message %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
                <% if (typeof error !== 'undefined' && error) { %>
                     <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="fas fa-exclamation-triangle me-2"></i>
                        <%= error %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>

                <div class="profile-card">
                    <div class="card-header-custom">
                        <h1>Manage Your Profile</h1>
                        <p class="text-muted mb-0">Keep your personal information up to date.</p>
                    </div>
                    <div class="card-body-custom">
                        <form action="/update-profile" method="POST">
                            <div class="mb-3">
                                <label for="fullName" class="form-label">Full Name</label>
                                <input type="text" class="form-control" id="fullName" name="fullName" value="<%= userData.full_name %>" required>
                            </div>
                            <div class="mb-4">
                                <label for="phoneNumber" class="form-label">Phone Number</label>
                                <input type="tel" class="form-control" id="phoneNumber" name="phoneNumber" value="<%= userData.phone_number %>" required>
                            </div>
                            
                            <hr class="my-4">

                            <h5 class="mb-3" style="color: var(--primary-blue-dark); font-weight: 600;">Change Password</h5>
                            <div class="mb-3">
                                <label for="newPassword" class="form-label">New Password</label>
                                <input type="password" class="form-control" id="newPassword" name="newPassword">
                                <small class="form-text password-note">Leave this blank if you don't want to change your password.</small>
                            </div>
                            <div class="mb-4">
                                <label for="confirmPassword" class="form-label">Confirm New Password</label>
                                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword">
                            </div>

                            <div class="d-grid mt-3">
                                <button type="submit" class="btn btn-primary fw-bold btn-update">Save Changes</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        lucide.createIcons();
    </script>
</body>
</html>
