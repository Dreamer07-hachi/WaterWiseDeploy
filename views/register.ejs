<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join SmartWater - Create Account</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/login.css" />
</head>

<body>
    <%- include ('partials/header-prehome') %>
        <div class="body-container">
            <div
                class="left-panel d-none d-md-flex flex-column justify-content-center align-items-center text-black p-5">
                <h1>Save. Educate. Protect.</h1>
                <p class="text-center">Join the future of reducing water wastage</p>
                <a href="/login">
                    <button id="showLogin" class="btn btn-light mt-3 rounded-pill px-4">Login</button>
                </a>
            </div>

            <div class="main-container right-panel">
                <div class="floating-card">
                    <div class="card-header">
                        <h2 class="card-title">Join the Change</h2>
                        <p class="card-subtitle">Create an account to manage your water usage</p>
                    </div>
                    <% if (error) { %>
                        <div class="alert alert-danger" role="alert">
                            <%= error %>
                        </div>
                        <% } %>

                            <form action="/register" method="POST">
                                <div class="form-group">
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-user"></i></span>
                                        <input type="text" class="form-control" name="fullName" placeholder="Full Name"
                                            required>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-phone"></i></span>
                                        <input type="tel" class="form-control" name="phoneNumber"
                                            placeholder="Phone Number" pattern="[0-9]{10}" maxlength="10"
                                            inputmode="numeric" required>
                                    </div>
                                </div>
                                <div class="form-group">
                                    <div class="input-group">
                                        <span class="input-group-text"><i class="fas fa-lock"></i></span>
                                        <input type="password" class="form-control" name="password"
                                            placeholder="Create Password" required>
                                    </div>
                                </div>
                                <button type="submit" class="btn btn-primary-custom">
                                    <i class="fas fa-rocket me-2"></i> Create Account
                                </button>
                            </form>

                            <div class="divider">
                                <span>or continue with</span>
                            </div>
                            <a class="btn btn-google-custom" href="#">
                                <i class="fab fa-google"></i> Sign up with Google
                            </a>
                </div>
            </div>
        </div>
        <%- include ('partials/footer-prehome') %>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>