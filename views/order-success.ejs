<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Successful - WaterWise</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        :root {
            --primary-blue: #0d6efd;
            --primary-blue-dark: #0a58ca;
            --light-blue-bg: #f0f7ff;
            --white-bg: #ffffff;
            --text-dark: #212529;
            --text-muted: #6c757d;
            --border-color: #e9ecef;
            --success-color: #198754;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--light-blue-bg);
            color: var(--text-dark);
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        
        main {
            flex-grow: 1;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .success-card {
            background-color: var(--white-bg);
            border-radius: 15px;
            border: 1px solid var(--border-color);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.07);
            text-align: center;
            padding: 3rem;
            max-width: 500px;
            width: 100%;
        }
        
        .success-icon {
            font-size: 5rem;
            color: var(--success-color);
            margin-bottom: 1.5rem;
            line-height: 1;
        }

        .success-card h1 {
            color: var(--primary-blue-dark);
            font-weight: 700;
        }
        
        .success-card p {
            color: var(--text-muted);
            font-size: 1.1rem;
            margin-bottom: 2rem;
        }
        
        .btn-track {
            background: linear-gradient(135deg, var(--success-color), #146c43);
            border: none;
            font-weight: 600;
            padding: 0.8rem 1.5rem;
            font-size: 1.1rem;
            border-radius: 8px;
        }

        .btn-home {
            color: var(--primary-blue);
            border-color: var(--primary-blue);
            font-weight: 600;
            padding: 0.8rem 1.5rem;
            font-size: 1.1rem;
            border-radius: 8px;
        }
        .btn-home:hover {
            background-color: var(--primary-blue);
            color: var(--white-bg);
        }

        /* Responsive styles */
main {
    padding: clamp(1rem, 3vw, 2rem);
}

.success-card {
    padding: clamp(2rem, 5vw, 3rem);
    max-width: clamp(320px, 90vw, 500px);
    border-radius: clamp(12px, 2vw, 15px);
    margin: 0 1rem;
}

.success-icon {
    font-size: clamp(3.5rem, 10vw, 5rem);
    margin-bottom: clamp(1rem, 3vw, 1.5rem);
}

.success-card h1 {
    font-size: clamp(1.75rem, 5vw, 2.5rem);
    margin-bottom: clamp(0.75rem, 2vw, 1rem);
}

.success-card p {
    font-size: clamp(0.95rem, 2.5vw, 1.1rem);
    margin-bottom: clamp(1.5rem, 3vw, 2rem);
}

.btn-track,
.btn-home {
    padding: clamp(0.65rem, 2vw, 0.8rem) clamp(1.25rem, 3vw, 1.5rem);
    font-size: clamp(0.95rem, 2.5vw, 1.1rem);
    width: 100%;
    margin-bottom: 0.75rem;
}

/* Tablet breakpoint */
@media (max-width: 768px) {
    .success-card {
        padding: 2.5rem 1.5rem;
    }
    
    .success-icon {
        margin-bottom: 1.25rem;
    }
    
    .success-card p {
        margin-bottom: 1.75rem;
    }
}

/* Mobile breakpoint */
@media (max-width: 480px) {
    main {
        padding: 1rem 0.5rem;
    }
    
    .success-card {
        padding: 2rem 1.25rem;
        border-radius: 12px;
        margin: 0 0.5rem;
    }
    
    .success-icon {
        font-size: 3.5rem;
        margin-bottom: 1rem;
    }
    
    .success-card h1 {
        font-size: 1.75rem;
        margin-bottom: 0.75rem;
    }
    
    .success-card p {
        font-size: 0.95rem;
        margin-bottom: 1.5rem;
    }
    
    .btn-track,
    .btn-home {
        padding: 0.65rem 1rem;
        font-size: 0.95rem;
        margin-bottom: 0.65rem;
    }
}

/* Extra small devices */
@media (max-width: 360px) {
    .success-card {
        padding: 1.5rem 1rem;
    }
    
    .success-icon {
        font-size: 3rem;
    }
    
    .success-card h1 {
        font-size: 1.5rem;
    }
    
    .success-card p {
        font-size: 0.875rem;
    }
}
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <main class="container">
        <div class="success-card">
            <div class="success-icon">
                <i class="fas fa-check-circle"></i>
            </div>
            <h1 class="mb-3">Order Placed!</h1>
            <p>Your water tanker is on its way. You can now track the delivery in real-time.</p>
            <div class="d-grid gap-3">
                <a href="/track-order/<%= orderId %>" class="btn btn-success fw-bold btn-track">
                    <i class="fas fa-map-marker-alt me-2"></i>Track Your Order
                </a>
                <a href="/home" class="btn btn-outline-primary fw-bold btn-home">Back to Dashboard</a>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        lucide.createIcons();
    </script>
</body>
</html>
